@model IEnumerable<FlowHouse.Models.Oddeleni>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Jmeno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Budget)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Administrator)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Jmeno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Budget)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Administrator.Jmeno)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OddeleniID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OddeleniID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OddeleniID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
